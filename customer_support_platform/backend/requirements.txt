# ==================================================================
# AI Customer Support Platform - backend
# Lean, known-compatible pin set. Transitive deps resolved by pip.
# ==================================================================

# --- web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- data / ORM ---
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10          # PostgreSQL driver (optional; SQLite needs nothing)

# --- validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- auth ---
pyjwt[crypto]>=2.9.0
pwdlib[argon2]==0.2.1

# --- AI provider SDKs (only the configured one is used) ---
openai==1.59.6                   # Gemini / Groq / OpenRouter / OpenAI (OpenAI-compatible)
anthropic==1.3.0

# --- RAG ---
fastembed==0.8.0             # local ONNX embeddings (BAAI/bge-small-en-v1.5, 384-dim, no PyTorch)
numpy==2.2.1
pypdf==5.1.0

# --- data loading (knowledge base from Kaggle datasets); optional, only for scripts/build_kb.py ---
kaggle>=1.6

# --- testing ---
pytest==8.3.4
httpx==0.28.1
